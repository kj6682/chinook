spring:
  application:
    name: hops
  datasource:
    url: jdbc:postgresql://localhost:5432/catalog
    username: hr
    password: hr
    platform: POSTGRESQL
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
server:
    port: 8082


spring:
  application:
    name: hops
  datasource:
    url: jdbc:postgresql://localhost:5432/catalog
    username: hr
    password: hr
    platform: POSTGRESQL
  jpa:
    show-sql: true
server:
    port: 8082
---
spring:
  profiles: create
  jpa:
    hibernate:
      ddl-auto: create
---
spring:
  profiles: default
  jpa:
    hibernate:
      ddl-auto: update
